import { FilterCarmodelPipePipe } from './filter-carmodel-pipe.pipe';

describe('FilterCarmodelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarmodelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
